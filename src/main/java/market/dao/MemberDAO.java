package market.dao;

import java.util.List;

import market.model.MemberDTO;

public interface MemberDAO {
	/*
	 * int memberInsert(MemberDTO member); int memberEmailCheck(String m_email);
	 * MemberDTO loginCheck(String m_email); MemberDTO select(String m_email); int
	 * memberConfirmForm(MemberDTO member); int memberConfirm(MemberDTO member); int
	 * memberUpdate(MemberDTO member); MemberDTO passwdSearch(MemberDTO member);
	 * MemberDTO emailSearch(MemberDTO member); int deleteMember(MemberDTO member);
	 * List<MemberDTO> memberList(); int getTotal(MemberDTO member); int
	 * memberListCheck(String email);
	 */
	
}
