package market.service;

import java.util.List;

import market.model.MemberDTO;

public interface MemberService {
	/*
	 * int memberInsert(MemberDTO member); int memberEmailCheck(String m_email);
	 * MemberDTO loginCheck(String m_email); MemberDTO select(String m_email);
	 * MemberDTO passwdSearch(MemberDTO member); MemberDTO emailSearch(MemberDTO
	 * member); int memberConfirmForm(MemberDTO member); int memberUpdate(MemberDTO
	 * member); int memberConfirm(MemberDTO member); int deleteMember(MemberDTO
	 * member); List<MemberDTO> memberList(); int memberListCheck(String email); int
	 * getTotal(MemberDTO member);
	 */
}
